import { colors } from '../../libs/theme';
import type { ITheme } from '../../libs/theme.type';

export const lightColorsTheme: ITheme = {
  primary: colors.primary,
  light: '#C5D3E0',
  secondary: colors.secondaryBackground,
  error: '#FF5252',
  warning: '#FFB155',
  success: '#30AF5B',
  background: colors.white,
};
